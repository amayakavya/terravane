name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  contracts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run compile
      - run: node scripts/checksize.js
      - run: npm test

  stack:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run compile
      - run: npm run css

      - name: Start a chain, deploy and seed
        run: |
          npx hardhat node > /tmp/chain.log 2>&1 &
          for i in $(seq 1 60); do
            curl -sf -X POST -H 'Content-Type: application/json' \
              --data '{"jsonrpc":"2.0","method":"eth_blockNumber","params":[],"id":1}' \
              http://127.0.0.1:8545 > /dev/null && break
            sleep 1
          done
          node scripts/deploy.js
          TERRAVANE_QUIET=1 node scripts/seed.js

      - name: Serve and exercise the API
        run: |
          node server/index.js > /tmp/server.log 2>&1 &
          for i in $(seq 1 60); do
            curl -sf http://127.0.0.1:4300/api/health | grep -q '"ready":true' && break
            sleep 1
          done
          node scripts/smoke.js

      - name: Load every page in a browser
        run: node scripts/checkui.js
